run = "python3 -m uvicorn app:app --host 0.0.0.0 --port 5000"

[deployment]
run = ["sh", "-c", "python3 -m uvicorn app:app --host 0.0.0.0 --port 5000"]
deploymentTarget = "cloudrun"
build = ["sh", "-c", "python3 -m pip install -r requirements.txt"]
