{
  "name": "chat-api-router",
  "version": "1.0.0",
  "description": "Cloudflare Workers router for chat API",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
